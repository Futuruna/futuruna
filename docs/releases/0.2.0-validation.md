# 0.2.0 integration validation

Status: **release candidate, not released**. Main must not be advanced or tagged
until the required integration gates are green. Track delivery in `td-54f105`,
gate stabilization in `td-e9e823`, and shared-import interpreter pattern parity
in `td-066cc7` (related ownership work: `td-625bb9`).

## Integration

- `feature/bounded-explore-spec` is an ancestor of
  `feature/explore-income-distance`; the latter was merged normally.
- The static-dispatch branch's only unique commit, `14ea6b89`, is patch-identical
  to `f1827009`, already in Explore. Both have stable patch ID
  `213ecc16b31330a4c8364bc21d01892215ad50f5`. Its ancestry-only merge preserves
  the integrated tree rather than replacing later compiler work.
- Version metadata and compatibility notes are prepared for 0.2.0. No existing
  worktree, branch, exploration checkpoint, or result bundle was deleted.

## Hosted evidence for c2f51d19

[Integration CI](https://github.com/Futuruna/futuruna/actions/runs/35310508678):
formatting, compatibility guide, and website SSG passed. Linux mint stopped at
the library suite: **744 passed, 16 failed, 7 ignored**. Matrix fail-fast cancelled
macOS mint; that is not passing macOS evidence. Canary, differential, and
downstream lanes also failed on generated-code regressions.

[Release dry run](https://github.com/Futuruna/futuruna/actions/runs/35310520940):
package verification and all four native builds/smokes passed with Rust 1.94.0.
The crate is approximately 2.3 MB; its 100 paths exclude tests, examples,
research, website, wiki, and private workspace trees. Its VCS metadata binds
`c2f51d19461893239cb62b79ab728bb49592f173`. This dry run did not publish a crate,
create a release, or prove that the integration tests passed. It must be repeated
for the final corrected commit.

Downloaded artifacts have the expected ELF x86-64/AArch64 and Mach-O
arm64/x86-64 architectures. Both macOS signing-status files report `false`;
these candidates are not Apple Developer ID signed or notarized. The Apple
Silicon candidate reports `runa 0.2.0` locally.

## Focused follow-up corrections

The resource-governor assertions were updated to the intentionally implemented
20% millicore reserve (1,200 on six cores; 3,200 on sixteen), preserving worker
ceilings. The standalone module test suite passes all 31 tests.

Two further corrections restore integer-collection `sum` backend typing and
root/plain/qualified shared-import codegen. They do not broaden exact proof
authority or implement general cross-parent ownership. With a single-job local
build, debug symbols and incremental compilation disabled:

- `runa test --run tests/canary/core`: 12 passed.
- `runa test --run tests/differential/corpus/imports`: 4 passed.
- `runa test --run tests/downstream`: 13 passed.
- `runa test --roundtrip tests/differential/corpus`: 6 matched, zero skipped.
- New integer-sum backend metadata unit test: passed, including unsupported
  element types and an authored function shadowing the intrinsic.
- New shared product-type import unit test: passed in both import orders,
  comparing interpreter and generated Rust.
- New differential fixture: formatting, checking, interpretation, and native
  roundtrip passed (outputs `2` and `8`).
- `cargo fmt --all -- --check` and `git diff --check`: passed.

The broader `compiled_qualified_` unit selection exposed two further failing
partial-rule contract tests. A first enum-pattern form of the new import test
also exposed interpreter output `()` versus compiled `7`. The final product
fixture isolates the codegen ABI fix; **enum-pattern parity is still a release
blocker**, recorded in `td-066cc7`, not a passing claim.

## Initial library failures (now passing in focused checks)

These were reproduced locally and corrected against current contracts. The
focused selection now passes 18 tests, including new executable malformed-domain
and shared-import collection regressions. Full integration gates remain required.

- `checked_query_identity_uses_revalidated_relational_identity_ladder`
- `plural_find_report_and_publication_keep_each_named_question_explicit`
- `outer_contained_swap_growth_keeps_permit_authority_and_never_names_swap_backoff`
- `canonical_root_is_independent_of_arrival_order`
- `canonical_collection_domains_preserve_checked_range_and_member_failures`
- `canonical_imports_keep_finite_types_collections_and_prefix_scope`
- `canonical_values_domains_fail_closed_for_unbounded_ambiguous_and_open_types`
- `canonical_rule_dispatch_registry_is_scoped_typed_and_ordered`
- `canonical_rule_dispatch_return_types_are_exact_and_fail_closed`
- `checked_analysis_sequence_preserves_plain_and_hash_origins_but_not_qualified_contents`
- `checked_resolution_pipe_recurses_all_transform_shapes_and_missing_sites_fail_closed`
- `checked_resolution_requires_exact_constructor_identity_and_binds_nullary_tags`
- `exact_exploration_initialization_keeps_transitive_root_callables_isolated`
- `interpreted_rulescope_memoizes_zero_argument_sibling_rules`
- `interpreted_typed_boolean_rule_misses_use_exact_return_metadata`
- `nested_data_constructor_still_collides_with_bare_actor_message`

The broader qualified-unit failures were
`compiled_qualified_import_rules_stay_in_each_module_namespace` and
`compiled_qualified_rulescope_uses_module_layout_and_members`. Their namespace
fixtures now author explicit value-rule fallbacks, preserving the separate
strict rejection tests. The qualified selection now passes all 20 tests. The new
shared-enum regression passes interpreter/native comparison in both import
orders (pattern matching, typed rule heads, constructed/early-value equality).

### Stabilization contract

- Shared plain/hash ADT imports retain constructor declaration provenance;
  direct qualified aliases and different source files remain distinct. Equality
  and collection hashing agree even for keys constructed before the root import.
  Root values and existing Explore value encodings are unchanged.
- Bare mixed-arity constructor names fail closed as exact values, including
  contextually annotated uses. Arithmetic backend typing does not certify
  potentially overflowing operations as total.
- Fully enumerated plural questions can publish classification summaries from
  their exact extensional seals without inventing a symbolic-support closure.
  Case-support graph v5 versions this changed authority contract; old v4 output
  directories must not be reused. No saved checkpoint/result bundle was changed.
- Symbolic collections remain deferred during elaboration. Permanent executable
  stream tests now assert rejection of malformed members and open RuleScopes
  at consumption; reversed/oversized ranges and import-prefix capture retain
  their separate ground-evaluation guards. Stream binding-plan preparation also
  rejects imported bindings that capture later root declarations, with a
  dedicated executable preparation regression. Nested failure causes are preserved.
- Remaining fixture corrections cover current syntax, canonical AST paths,
  platform-specific telemetry, dependency-valid cursor insertion, exact arity,
  declaration-environment memo guards, and isolated nested constructors.

Local iteration uses `CARGO_BUILD_JOBS=1 CARGO_PROFILE_DEV_DEBUG=0
CARGO_PROFILE_TEST_DEBUG=0 CARGO_INCREMENTAL=0 cargo test --locked` with focused
library/bin filters, followed by the built library harness's named regression
selection (`--test-threads 1`). It does not substitute for full mint or deeper CI.

The new `tests/differential/corpus/imports/shared_import_enum.runa` passes
`runa fmt --check`, `runa check`, interpretation, and `runa run`; both backends
print `review` then `ship`. Generic `runa test --roundtrip` explicitly skips
import entrypoints (all five import files skipped), so that command is not
claimed as parity evidence. The differential script's import-aware native and
codegen lanes include the new fixture.

After stabilization, run full mint, differential, canary, downstream, website,
and four-target release preflight on the final candidate. Only then merge to
main, create the signed version tag, publish, and verify the released assets and
checksums. Existing key availability is not evidence of Apple notarization or
GitHub account-level signature verification.
