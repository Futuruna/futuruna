-- roundtrip-skip: library-style WASM smoke fixture, no runtime output to compare
-- Minimal end-to-end WASM smoke fixture

> add(a: Int, b: Int) -> Int { a + b }
@ export add

> greet(name: String) -> String { "Hello, " + name + "!" }
@ export greet
