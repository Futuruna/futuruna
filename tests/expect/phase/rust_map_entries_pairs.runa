-- expect-command: emit-rust
-- expect-status: pass
-- expect-stdout: fn entry_label(entry: &(String, i64)) -> String
-- expect-stdout: entry.0.clone()
-- expect-stdout: iter().map(|(k, v)| (k.clone(), v.clone())).collect::<Vec<_>>()
> entry_label(entry: Pair(String, Int)) -> String {
    entry.fst + ":" + show(entry.snd)
}

= counts0 = map_new()
= counts1 = map_insert(counts0, "north", 2)
= counts2 = map_insert(counts1, "west", 3)
= entries = map_entries(counts2)
= label = entry_label(head(entries))
