-- expect-command: check
-- expect-status: fail
-- expect-stderr: Futuruna uses `->` for return types, not `=>`.
-- expect-stderr: Try: > add(a, b) -> ReturnType { body }
> add(a: Int, b: Int) => Int { a + b }
