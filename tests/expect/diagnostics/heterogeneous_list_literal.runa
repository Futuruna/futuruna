-- expect-command: check
-- expect-status: fail
-- expect-stderr: list elements must have the same type; first element is `Int` but this element is `String`
-- expect-stderr: = values = [1, "two"]
= values = [1, "two"]
