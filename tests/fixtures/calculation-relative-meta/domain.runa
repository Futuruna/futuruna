# ImportedChild(age: Int)
# RelativeField(path: ProgramReference, label: String)
# RelativeMeta(fields: List(RelativeField))
# impl Meta for RelativeMeta {}

= imported_child_meta = RelativeMeta(fields = [
    RelativeField(path = refof(ImportedChild::age), label = "Imported child age")
])

--@label:ImportedChild::meta:imported_child_meta--
