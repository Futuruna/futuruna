@ import ./registry

# SourceMetaRole(a) = Source(value: a)
# Metadata(a) = Metadata(attachments: a)
# impl MetaRole for SourceMetaRole {}
# impl Meta for Metadata {}

= imported_metadata = Metadata(
    attachments = (
        Source(value = registry_source),
    )
)

--@label:imported_aggregate::meta:imported_metadata--
