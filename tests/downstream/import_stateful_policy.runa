-- library-hygiene: importable
-- roundtrip-skip: library file, no expected output

@ export
> counts_balanced(snapshot: String) -> Bool {
    contains(snapshot, "pages=3") && contains(snapshot, "channels=3")
}

@ export
> latests_ready(snapshot: String) -> Bool {
    contains(snapshot, "latest_page=score") && contains(snapshot, "latest_channel=queue")
}
