[package]
name = "futuruna-web"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.7", features = ["web", "router"] }
futuruna = { path = ".." }
gloo-timers = { version = "0.3", features = ["futures"] }
pulldown-cmark = "0.12"
